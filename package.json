{
    "name": "Natasha Williams",
    "private": true,
    "description": "Natasha Williams portfolio",
    "version": "0.1.0",
    "license": "0BSD",
    "scripts": {
        "build": "gatsby build",
        "deploy": "gatsby build --prefix-paths && gh-pages -d public -b public",
        "develop": "gatsby develop",
        "format": "prettier --write \"**/*.{js,jsx,ts,tsx,json,md}\"",
        "start": "npm run develop",
        "serve": "gatsby serve",
        "clean": "gatsby clean",
        "test": "echo \"Write tests! -> https://gatsby.dev/unit-testing\" && exit 1"
    },
    "dependencies": {
        "gatsby": "^4.14.0",
        "gatsby-plugin-react-helmet": "^5.14.0",
        "gatsby-plugin-sass": "^5.14.0",
        "gatsby-plugin-scroll-reveal": "^0.0.7",
        "react": "^18.1.0",
        "react-dom": "^18.1.0",
        "react-helmet": "^6.1.0"
    },
    "devDependencies": {
        "gh-pages": "^4.0.0",
        "sass": "^1.51.0"
    }
}
